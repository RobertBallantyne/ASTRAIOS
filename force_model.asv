function dydt = force_model(t, state)

x = state(1);
y = state(2);
z = state(3);

vx = state(4);
vy = state(5);
vz = state(6);

gx, gy, gz = gravity(x, y, z);

ax = gx;
ay = gy;
az = gz;

dydt = [vx; vy; vz; ax; ay; az];
end